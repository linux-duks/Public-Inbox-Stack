# Grokmirror upstream source (where you mirror FROM)
UPSTREAM_HOST=lore.example.org

# Public-inbox serving URL (where you serve TO)
SERVE_HOST=localhost

# ACME/SSL configuration
ACME_ENABLED=false
ACME_EMAIL=admin@example.com
